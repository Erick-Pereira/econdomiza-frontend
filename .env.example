# URL base do gateway (sem barra final). Em dev pode usar proxy do Vite — ver vite.config.ts
VITE_SIMCAG_GATEWAY_URL=http://localhost:5000
# Opcional: pré-preencher o campo Tenant ID no ecrã de login (GUID)
# VITE_DEFAULT_TENANT_ID=00000000-0000-0000-0000-000000000000
